# Example environment configuration for Job Tracker

# PostgreSQL connection string (e.g. from Neon)
DATABASE_URL=postgresql://your_user:your_password@your-host.neon.tech/your_db?sslmode=require

# Secret key used to sign JWT tokens
JWT_SECRET=your_jwt_secret

# Port on which the Express server will run
PORT=3000
